import 'package:flutter/material.dart';

class Google_Map extends StatelessWidget {
  const Google_Map({super.key});

  @override
  Widget build(BuildContext context) {
    return const Material(
      child: Center(
        child: Text("Hello This is map"),
      ),
    );
  }
}


